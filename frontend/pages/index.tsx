import type { NextPage } from 'next';
import { Title } from '@mantine/core';

const Home: NextPage = () => {
  return (
    <div>
      <Title order={1}>Hello World</Title>
    </div>
  );
};

export default Home;
